<h1>MOVIE RESULTS PAGE!!!</h1>
<ul>
    <% results["Search"].forEach( function(movie){ %>
    
         <li>
             <strong><%= movie["Title"] %></strong> - <%= movie["Year"] %> - <a href="/results/<%= movie["imdbID"] %>" > more..</a>
         </li>
    
    <% }); %>

</ul>

<a href="/">Search Again!</a>